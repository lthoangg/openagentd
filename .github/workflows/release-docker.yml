# Docker image build and publish to GitHub Container Registry.
# Uncomment the entire workflow when ready to publish images.
#
# Triggers:
#   - Push to main: builds and pushes :latest tag
#   - Version tags (app-v*): builds and pushes :x.y.z tag
#
# Prerequisites:
#   - Repository must have packages write permission enabled
#   - No secrets needed — uses GITHUB_TOKEN automatically

# name: Docker
#
# on:
#   push:
#     branches: [main]
#     tags: ["app-v*"]
#     paths:
#       - "app/**"
#       - "web/**"
#       - "Dockerfile"
#       - "docker-compose.yaml"
#       - ".github/workflows/docker.yml"
#
# env:
#   REGISTRY: ghcr.io
#   IMAGE_NAME: ${{ github.repository }}
#
# jobs:
#   build-and-push:
#     name: Build & push image
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       packages: write
#
#     steps:
#       - uses: actions/checkout@v4
#
#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3
#
#       - name: Log in to GitHub Container Registry
#         uses: docker/login-action@v3
#         with:
#           registry: ${{ env.REGISTRY }}
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}
#
#       - name: Extract metadata (tags, labels)
#         id: meta
#         uses: docker/metadata-action@v5
#         with:
#           images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
#           tags: |
#             type=ref,event=branch
#             type=match,pattern=app-v(.*),group=1
#             type=sha,prefix=
#
#       - name: Build and push
#         uses: docker/build-push-action@v6
#         with:
#           context: .
#           push: true
#           tags: ${{ steps.meta.outputs.tags }}
#           labels: ${{ steps.meta.outputs.labels }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max
#           platforms: linux/amd64,linux/arm64
